{
	# email is only needed for HTTPS/ACME certificates
	# Set CADDY_EMAIL env var if using automatic HTTPS
	email {$CADDY_EMAIL:admin@localhost}
}

{$CADDY_SITE:http://localhost} {
	encode gzip zstd
	reverse_proxy /api* api:5078
	reverse_proxy web:3000
}
